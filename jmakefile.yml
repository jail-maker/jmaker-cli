---
name: "jmaker-cli"
from: "freebsd-11.1"
ip4.addr: "auto"

env:
  ASSUME_ALWAYS_YES: "yes"

copy:
  - ["./.git", "/usr/local/project/.git"]

exec.j-prestart:
  - "pkg install -y nano"
  # - "pkg install -y i3 i3lock i3status"
  # - "pkg install -y xf86-video-qxl"
  # - "pw useradd -n user -s /bin/csh -m -w random"

exec.j-poststart:
  - "ping ya.ru"
  # - "service dbus onestart"
  # - "service hald onestart"
  # - "su - user startx"
