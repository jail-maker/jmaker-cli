---
name: "test-jail"
base: "freebsd-11.1"
ip4.addr:
  - "?|192.168.0.33/24"

mounts:
  - ['./', '/mnt']

exec.start:
  - "/bin/sh /etc/rc"
  - "/mnt/cpconfigs.sh"

pkg:
  - "apache24"
  - "php70"

dependencies:
  - "web-layer"

env:
  dhcp:
    ip4.addr: "DHCP"

  auto:
    ip4.addr: "AUTO"
